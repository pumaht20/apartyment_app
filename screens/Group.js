import * as React from "react";
import {
  StyleSheet,
  Text,
  Button,
  View,
  SafeAreaView,
  ScrollView,
} from "react-native";

const Group = ({ navigation }) => {
  return <Text>This is in Group </Text>;
};

export default Group;
