import * as React from "react";
import { StyleSheet, Text, Button, View } from "react-native";

const CreateEvent = () => {
  return <Text>This is in CreateEvent</Text>;
};

export default CreateEvent;
