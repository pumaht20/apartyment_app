import * as React from "react";
import { StyleSheet, Text, Button, View } from "react-native";

const ScheduleCard = () => {
  return <Text>This is a schedule card!</Text>;
};

export default ScheduleCard;
